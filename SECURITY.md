# Security

Please do not report security vulnerabilities in public issues.

Do not commit real `.env` files, API keys, AWS credentials, JWT secrets, database URLs, certificates, tokens, customer data, or production deployment config.

Use `.env.example` files for safe placeholders only.
