{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bimtools.com/schemas/revit-safe-read-proof-receipt-v1.json",
  "title": "Revit Safe Read proof receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "proofKind",
    "mode",
    "status",
    "certified",
    "manifestSha256",
    "trustBoundary",
    "compilerOptions",
    "issues",
    "observation",
    "artifacts"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "proofKind": { "const": "revit-safe-read-whole-assembly/v1" },
    "mode": { "enum": ["check", "inventory"] },
    "status": { "enum": ["verified", "observed", "rejected"] },
    "certified": { "type": "boolean" },
    "manifestSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "trustBoundary": { "type": "string", "minLength": 1 },
    "compilerOptions": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" }
    },
    "issues": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["code", "message"],
        "properties": {
          "code": { "type": "string", "pattern": "^[A-Z0-9_]+$" },
          "message": { "type": "string", "minLength": 1 }
        }
      }
    },
    "observation": {
      "$ref": "#/$defs/observation"
    },
    "artifacts": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["fileName", "sha256", "length"],
        "properties": {
          "fileName": { "type": "string", "minLength": 1 },
          "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "length": { "type": "integer", "minimum": 1 }
        }
      }
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "inventory": {
      "type": "object",
      "additionalProperties": false,
      "required": ["count", "sha256", "items"],
      "properties": {
        "count": { "type": "integer", "minimum": 0 },
        "sha256": { "$ref": "#/$defs/sha256" },
        "items": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "variant": {
      "type": "object",
      "additionalProperties": false,
      "required": ["references", "symbolBindings", "methods", "metadata", "il", "outputSha256"],
      "properties": {
        "references": { "$ref": "#/$defs/inventory" },
        "symbolBindings": { "$ref": "#/$defs/inventory" },
        "methods": { "$ref": "#/$defs/inventory" },
        "metadata": { "$ref": "#/$defs/inventory" },
        "il": { "$ref": "#/$defs/inventory" },
        "outputSha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "observation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["syntax", "types", "members", "serializationClosure", "resources", "variants"],
      "properties": {
        "syntax": { "$ref": "#/$defs/inventory" },
        "types": { "$ref": "#/$defs/inventory" },
        "members": { "$ref": "#/$defs/inventory" },
        "serializationClosure": { "$ref": "#/$defs/inventory" },
        "resources": { "$ref": "#/$defs/inventory" },
        "variants": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/variant" },
          "propertyNames": { "enum": ["2023", "2024", "2025"] }
        }
      }
    }
  }
}
