# Security Model

The public core supports local and self-hosted operation.

Public configuration must use placeholders only. Runtime secrets should come from local environment variables, secret stores, or deployment systems.

Hosted BIMTools authentication, billing, licensing, and customer-specific policy belong in the private repo.
