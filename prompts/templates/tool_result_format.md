# Template: Tool results (user-facing)

When reporting tool results:
- Summarize the outcome in 1–3 lines.
- Include the exact identifiers (element ids, sheet numbers, view names) returned by tools.
- For files: include the exact workspace-relative path and an `op://open-folder` link.
- If a tool failed: include the exact error string.

